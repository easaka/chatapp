<div class="container">
    <h5><%= current_user.email%></h5>

    <h4>User</h4>
    <%= turbo_stream_from "users"%>
    <div id="users">
        <%= render @users %>
    </div>
    <h4>Rooms</h4>
    <%= render 'layouts/new_room_form' %>
    <%= turbo_stream_from "rooms"%>
    <div id="rooms">
        <%= render @rooms %>
    </div>

</div>
